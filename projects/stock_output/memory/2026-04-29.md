# 2026-04-29 工作日志

## sync.ps1 全面动态化改造
- **C 层 Skills**：从固定列表（10个）改为自动扫描 `~/.workbuddy/skills/` 全部子目录，首次补齐 14 个遗漏的 Skill，总计 24 个
- **D 层项目知识**：从固定 5 条路径改为自动扫描 `$ProjectScanRoots`（当前为 `G:\`）下所有含 `.workbuddy` 的项目，自动同步其 memory/learning/scripts 子目录
  - 首次发现并同步了之前遗漏的 `project_output` 项目，总计 4 个项目 6 个子目录
  - 家里电脑只需改 `$ProjectScanRoots` 的盘符即可
- 新增内容在日志中以 `[NEW SKILL]` / `[NEW PROJECT]` 高亮提示
- A 层（3个固定身份文件）、B 层（2个固定记忆目录）、E 层（知识库路径不可预测）保持写死，更安全
- 已推送到 GitHub（→ a27bba7）
